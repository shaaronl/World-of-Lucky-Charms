import processing.core.PImage;

import java.util.List;

public class HeartCharm extends LuckyCharm{
    public HeartCharm(String id, Point position, List<PImage> images, double animationPeriod) {
        super(id, position, images, animationPeriod);
    }
}
